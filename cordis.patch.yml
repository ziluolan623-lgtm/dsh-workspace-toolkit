# dsh bundle patch: inserts this plugin into a profile's layer stack.
# The client half needs no row here — dsh's clientModules scans loader
# entries for packages declaring `dsh.client` and serves ./client/client.js
# at /plugins/dsh-workspace-toolkit/client.js automatically.
- insert:
    - id: workspace-toolkit
      name: dsh-workspace-toolkit
